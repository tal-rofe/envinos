import figures from 'figures';
import chalk from 'chalk';

export const ERROR_PREFIX = chalk.redBright(`${figures.cross} [Envinos]:`);

export const WARNING_PREFIX = chalk.yellowBright(`${figures.warning} [Envinos]:`);
